# Frontend .env example (opcional)
# Angular no usa .env por defecto, pero puedes exponer variables con prefijo NG_APP_
# y leerlas en tiempo de construcción o con un pequeño servicio si decides refactorizar.

# URL base de la API (para futuro refactor)
NG_APP_API_BASE_URL=http://localhost:8000

# Nombre de la aplicación (branding)
NG_APP_APP_NAME=BLS Academy

# Otros flags que podrías querer usar
NG_APP_ENABLE_ANALYTICS=false
